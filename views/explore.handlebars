<!DOCTYPE html>
<html lang="en">

  

<head>
    <title>Explore Exercises</title>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="https://unpkg.com/@picocss/pico@1.0.2/css/pico.min.css">
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="../css/style.css" />
    <!-- <script src="https://cdn.jsdelivr.net/npm/dotenv@10.0.0/dist/browser.js"></script> -->
</head>

<body>
  <section class="flex-row justify-center">

    <form action="/explore" method="GET">
      <label for="muscleGroup">Select a muscle group:</label>
      <select name="muscleGroup" id="muscleGroup">
        {{#each muscleGroups}}
          <option value="{{value}}" id="{{id}}">{{label}}</option>
        {{/each}}
      </select>
    </form>

    {{!-- <a href="/explore"></a> --}}

    <button class="btn-red" id="getExercises">Get Exercises! [Call API]</button>
    <button class="btn-red" id="getRandomExercises">Get Random Exercises! [Call API]</button>
    {{!-- the below two buttons were for testing purposes and can be deleted when we are done --}}
    {{!-- <button class="btn-red" id="getAllExercises">Get All Exercises! [Call API]</button>
    <button id="retrieve">Retrieve from local storage</button> --}}

    <div class="m-4" id="exerciseContainer">
      {{#each exercises}}
        <div class="card">
          <div class="card-header">
            <h2 id="exerciseName"><strong>{{toTitleCase name}}</strong></h2>
          </div>
          <div class="card-body">
            <p id="exerciseMuscle"><strong>Muscle Group: </strong>{{toTitleCase muscle}}</p>
            <p id="exerciseEquipment"><strong>Equipment: </strong>{{toTitleCase equipment}}</p>
            <p id="exerciseDifficulty"><strong>Difficulty: </strong>{{toTitleCase difficulty}}</p>
            <p id="exerciseInstructions"><strong>Instructions: </strong>{{instructions}}</p>
          </div>
          <p id="messageUser"></p>
        </div>
      {{/each}}
    </div>
  </section>

  <script src="https://cdn.jsdelivr.net/npm/handlebars@4.7.7/dist/handlebars.min.js"></script>
  <script src="/js/script.js" type="text/javascript"></script>
</body>

</html>
