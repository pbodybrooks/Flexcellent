<!DOCTYPE html>
<html lang="en">

<head>
    <title>Explore Exercises | Flexcellent</title>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="https://unpkg.com/@picocss/pico@1.0.2/css/pico.min.css">
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/css/explore-stylesheet.css" />
    <link rel="stylesheet" href="/css/master-stylesheet.css" />
</head>

<body>
  <section class="flex-row justify-center">

    <form action="/explore" method="GET">
      <label for="muscleGroup">Select a muscle group:</label>
      <select name="muscleGroup" id="muscleGroup">
        {{#each muscleGroups}}
          <option value="{{value}}" id="{{id}}">{{label}}</option>
        {{/each}}
      </select>
    </form>

    
    <div>
      <button class="btn-red neon-glow-button" id="getExercises">Get Exercises!</button>
      <button class="btn-red neon-glow-button" id="getRandomExercises">Get Random Exercises!</button>
    
    </div>

    <div class="m-4" id="exerciseContainer">
      {{#each exercises}}
        <div class="card">
          <div class="card-header">
            <h2 class="neon-glow-text" id="exerciseName"><strong>{{toTitleCase name}}</strong></h2>
          </div>
          <div class="card-body">
            <p id="exerciseMuscle"><strong>Muscle Group: </strong>{{toTitleCase muscle}}</p>
            <p id="exerciseEquipment"><strong>Equipment: </strong>{{toTitleCase equipment}}</p>
            <p id="exerciseDifficulty"><strong>Difficulty: </strong>{{toTitleCase difficulty}}</p>
            <p id="exerciseInstructions"><strong>Instructions: </strong>{{instructions}}</p>
          </div>
          <p id="messageUser"></p>
        </div>
      {{/each}}
    </div>
  </section>

  <script src="https://cdn.jsdelivr.net/npm/handlebars@4.7.7/dist/handlebars.min.js"></script>
  <script src="/js/explore.js" type="text/javascript"></script>
</body>

</html>
