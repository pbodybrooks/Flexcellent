<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Workout History | Flexcellent</title>
        <meta charset="UTF-8" />
        <link rel="stylesheet" href="https://unpkg.com/@picocss/pico@1.0.2/css/pico.min.css">
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="stylesheet" href="/css/workout-history-stylesheet.css" />
        <link rel="stylesheet" href="/css/master-stylesheet.css" />
    </head>

    <body>
        <div class="header">
            <h1 class="neon-glow-text">Workout History</h1>
        </div>
        <div>
            <button class="btn-red neon-glow-button" id="addWorkoutBtn">Add a Custom Workout</button>
        </div>
        <div id="workoutHistoryContainer">
            {{#each result}}
                <div class="workoutCard">
                    <div class="cardHeader">
                        <h2 class="neon-glow-text" id="workoutName"><strong>{{workoutData.name}}</strong></h2>
                        <h3 class="neon-glow-text" id="workoutDate"> {{format_date_day_of_week workoutData.date_created}}</h3>
                        {{#if workoutData.workout_duration}}
                            <h3 class="neon-glow-text" id="workoutDuration">Duration: {{workoutData.workout_duration}} minutes</h3>
                        {{/if}}
                    </div>
                    <div class="cardBody">
                        {{#each exercises}}
                            <div class="exerciseSubCard">
                                <h4 id="exerciseHeader">{{this.name}}</h4>
                                <p id="exerciseNumber">Exercise {{addHelper @index 1}}</p>
                                <ul id="exerciseDetails">
                                    <li><strong>Primary Muscle Group:</strong> {{this.muscle}}</li>
                                    <li>{{this.sets}}  <strong>Sets of</strong>  {{this.reps}}  <strong>Reps</strong></li>
                                    <li><strong>Equipment Weight:</strong> {{this.equipment_weight}} lbs</li>
                                </ul>
                            </div>
                        {{/each}}
                    </div>
                </div>
            {{/each}}
        </div>


        <script src="https://cdn.jsdelivr.net/npm/handlebars@4.7.7/dist/handlebars.min.js"></script>
        <script src="/js/workoutHistory.js" type="text/javascript"></script>
    </body>
</html>
